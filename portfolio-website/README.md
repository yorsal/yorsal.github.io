# 个人作品集网站

一个专业、现代的个人公司宣传网站，使用 Vite + React + Tailwind CSS + GSAP 构建。

## 功能特性

- ✨ **GSAP 滚动动画** - 流畅的滚动触发动画效果
- 🌙 **Dark Mode** - 支持深色模式切换
- 🌍 **中英文切换** - 完整的多语言支持
- 📱 **响应式设计** - 完美适配各种设备
- 🎨 **现代UI设计** - 基于 Tailwind CSS 的精美界面
- 📧 **联系表单** - 使用 EmailJS 发送邮件功能

## 技术栈

- **Vite** - 快速的前端构建工具
- **React** - UI 框架
- **Tailwind CSS** - 实用优先的 CSS 框架
- **GSAP** - 强大的动画库
- **Lucide React** - 精美的图标库

## 开始使用

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 项目结构

```
portfolio-website/
├── public/
│   └── 杨乐简历.pdf
├── src/
│   ├── components/      # React 组件
│   │   ├── Header.jsx
│   │   ├── Hero.jsx
│   │   ├── About.jsx
│   │   ├── Services.jsx
│   │   ├── Projects.jsx
│   │   ├── Contact.jsx
│   │   └── Footer.jsx
│   ├── contexts/        # React Context
│   │   ├── ThemeContext.jsx
│   │   └── LanguageContext.jsx
│   ├── data/           # 数据文件
│   │   └── translations.js
│   ├── App.jsx         # 主应用组件
│   ├── main.jsx        # 入口文件
│   └── index.css       # 全局样式
├── tailwind.config.js
├── postcss.config.js
└── vite.config.js
```

## 功能说明

### 主题切换
- 点击导航栏中的月亮/太阳图标切换深色/浅色模式
- 主题偏好会保存在 localStorage 中

### 语言切换
- 点击导航栏中的 "EN" 或 "中" 按钮切换语言
- 语言偏好会保存在 localStorage 中

### GSAP 动画
- Hero 区域：淡入和缩放动画
- 其他区域：滚动触发动画，元素依次出现
- 背景渐变：随滚动移动的渐变效果

## 自定义

### 修改内容
编辑 `src/data/translations.js` 文件来更新网站内容。

### 修改样式
编辑 `tailwind.config.js` 来定制主题颜色和字体。

### 添加项目
在 `src/components/Projects.jsx` 中的 `projects` 数组添加新项目。

## 许可证

MIT
